# BiasClear API — Pinned Dependencies
# Runtime/API dependencies only.
# For reviewer or full test setup, use:
#   pip install -e ".[api,dev]"
# CI uses the same extras on Python 3.12.
# Last pinned: 2026-02-21
# Update via: pip install --upgrade -r requirements.txt && pip freeze | grep ...
fastapi==0.135.2
uvicorn[standard]==0.41.0
pydantic==2.12.5
google-genai==1.66.0
boto3==1.38.27
python-dotenv==1.2.1
diff-match-patch==20241021
